package oop;
public class Acrylic extends Painting {
    public int layers;
    public Acrylic(String title, int layers) {
        super(title);
        this.layers = layers;
    }
    public void addLayer() {
        this.layers += 1;
    }
    
}